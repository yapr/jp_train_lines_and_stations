# frozen_string_literal: true

RSpec.describe JpTrainLinesAndStations do
  it "has a version number" do
    expect(JpTrainLinesAndStations::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
